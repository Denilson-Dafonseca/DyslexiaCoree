{
    "builds": [
        {
            "src": "Dyslexia/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb",
                "runtime": "python3.12",
                "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
            }
        }
    ],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/api/health",
            "dest": "Dyslexia/wsgi.py",
            "headers": {
                "x-vercel-test": "true"
            }
        },
        {
            "src": "/(.*)",
            "dest": "Dyslexia/wsgi.py"
        }
    ]
}